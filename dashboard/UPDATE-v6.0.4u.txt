Gustavo's Cointab Dashboard v6.0.4u
APP SIZE + MOBILE RESPONSIVE FIX

- Location UNINSTALL APPS rows now show reported app size.
- Per-tablet uninstall rows also show reported app size.
- Accepts app size fields: sizeBytes, appSizeBytes, installedSizeBytes,
  totalSizeBytes, packageSizeBytes, bytes, or size.
- If the current Launcher does not report a size yet, the dashboard displays
  SIZE NOT REPORTED rather than inventing a value.
- On phones the sidebar/navigation, header, location action buttons, tablet cards,
  uninstall modal, search box, app rows, and action buttons now stack cleanly.
- At very narrow widths location actions become one button per row.
- Preserves v6.0.4t location sync/uninstall and 1-second dashboard behavior.
